### T4 思想实验记录：传感器冲突与意图觉醒

**1. 现象描述**：
当传感器发生“虚假坠落”上报时，底层 PID 会因 D 项制动产生错误动作。

**2. 逻辑策略（故障容错）**：
- **符号识别**：利用“信号整体性不匹配”推断硬件故障。
- **降级意图**：生成 `HARDWARE_CHECK` 符号，执行最小化动作集的安全排查。

**3. 结论**：
意图的产生不应只依赖单一传感器信号，而应基于多个符号的“逻辑共识”。