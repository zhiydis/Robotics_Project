# robotics - 现代控制理论核心架构与工程落地全手册

本项目致力于彻底抛弃“黑盒”思维，将控制理论与实际工程落地（如 ROS 2 底座、机器人控制）深度结合，构建从数学矩阵到物理实体的直观映射。

---

## 📌 核心模块一：状态空间建模与矩阵的物理映射

在现代控制理论中，我们通过状态空间方程来描述系统的隐秘细节：

$$\dot{x} = Ax + Bu$$
$$y = Cx + Du$$

### 1. 矩阵的静态本质与变量的动态物理意义
* **状态向量 $x$（活的数据）**：系统内部真实的物理变量（如叉车的位置 $x_1$、速度 $x_2$）。它们随时间疯狂变化，代表实时运行轨迹。
* **系统骨架 $A$ 矩阵（死的硬件）**：代表物理世界天生的结构和惯性摩擦。一旦机械结构敲定，数字永远不会改变。 
* **控制肌肉 $B$ 矩阵（死的硬件）**：代表执行器（电机）的安装位置和传导比例。
* **传感器镜片 $C$ 矩阵（死的硬件）**：代表传感器的安装通道和电子放大倍数。

> **工程排雷铁律**：$A, B, C$ 矩阵在“线性定常系统”中是绝对的**静态常数**。如果系统瞎了或暴走了，一定是这三个静态矩阵的设计出了致命盲区，导致动态数据 $x$ 传输出错。

---

## 📌 核心模块二：标准型矩阵的“镜像拼装”大法

将复杂的传递函数转换为标准的矩阵形式，是我们在代码中实现算法的第一步：

### 1. 能控标准型 (CCF - Controllable Canonical Form)
* **适用场景**：为系统设计底层控制器（极点配置）时使用。
* **特征**：$B$ 矩阵极其干净，前 $n-1$ 行为 0，最后一行为 1。即 $B = [0, 0, \dots, 1]^T$。

---

## 📌 核心模块三：极点配置与工程落地



通过状态反馈 $u = -Kx$，我们可以将闭环系统特征值（极点）配置到左半平面的任何位置，让原本发散暴走的系统变得平稳。

### 1. 极点配置的充要条件
* **终极铁律**：状态反馈能实现极点任意配置的充要条件是——原系统 $(A, B)$ 必须**完全能控**！如果系统不能控，说明物理硬件有死角，软件代码再牛也无法把死角状态拽到安全区。

### 2. 工程降维打击（拒绝手算）
在工程实战（如 Python/ROS 2 底座）中，无需手算，直接调用工业级 API 求解反馈矩阵 $K$：
```python
import scipy.signal

# A, B 矩阵及期望极点
K = scipy.signal.place_poles(A, B, desired_poles).gain_matrix