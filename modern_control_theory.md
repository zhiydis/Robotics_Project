# 现代控制理论核心架构与工程落地手册

> 版本：2.0（2026-09-07 重写）｜适用范围：机器人/平衡车/无人叉车等嵌入式控制系统的理论→工程落地
> 本文档系统整理现代控制理论的核心模块，涵盖状态空间描述、能观能控分析、李雅普诺夫稳定性、极点配置、状态观测器与 LQR 最优控制。各模块给出数学定义、物理含义与工程实现要点。

---

## 第一部分 状态空间描述与系统分析

### 1. 状态空间模型

古典控制理论基于输入-输出传递函数，属于"黑盒"视角；现代控制理论将系统展开为包含全部内部状态的一阶微分方程组，即状态空间描述：

$$
\dot{x} = A x + B u
$$

$$
y = C x + D u
$$

其中：

| 符号 | 名称 | 物理含义 |
|---|---|---|
| $x \in \mathbb{R}^n$ | 状态向量 | 系统内部真实物理量（位置、速度、电流等），随时间演化 |
| $u \in \mathbb{R}^m$ | 输入向量 | 执行器施加的控制量 |
| $y \in \mathbb{R}^p$ | 输出向量 | 传感器可测得的量 |
| $A$ | 系统矩阵 | 系统固有结构与动力学（惯性、摩擦、耦合），由机械结构决定 |
| $B$ | 输入矩阵 | 执行器（电机）的安装位置与作用通道 |
| $C$ | 输出矩阵 | 传感器的安装通道与增益 |
| $D$ | 前馈矩阵 | 输入对输出的直接通道（多数系统为零） |

**工程要点**：对线性定常系统（LTI），$A, B, C, D$ 为常数矩阵。若系统出现异常行为而 $x$ 的演化不符预期，应从系统矩阵的建模是否准确、传感器/执行器通道是否完备的角度排查，而非归咎于动态数据本身。

### 2. 能控标准型（Controllable Canonical Form, CCF）

将传递函数转化为标准矩阵形式是实现算法的第一步。能控标准型适用于**控制器（极点配置）设计**。

设传递函数分母多项式为 $D(s) = s^n + a_{n-1}s^{n-1} + \cdots + a_0$，则 CCF 具有如下结构：

$$
A = \begin{bmatrix}
0 & 1 & 0 & \cdots & 0 \\
0 & 0 & 1 & \cdots & 0 \\
\vdots & & & \ddots & \vdots \\
-a_0 & -a_1 & -a_2 & \cdots & -a_{n-1}
\end{bmatrix}, \quad
B = \begin{bmatrix} 0 \\ 0 \\ \vdots \\ 1 \end{bmatrix}
$$

- $A$ 矩阵：主对角线上方为 1 的移位结构（友矩阵），最后一行存放分母多项式系数（变号、升序排列）。
- $B$ 矩阵：除最后一项为 1 外均为 0，结构简洁。
- $C$ 矩阵：按正序吸收分子多项式系数。

### 3. 能观标准型（Observable Canonical Form, OCF）

能观标准型适用于**状态观测器设计**，与 CCF 构成对偶结构：

- $C$ 矩阵：除最后一列为 1 外均为 0。
- $A$ 矩阵：主对角线**下方**为 1 的移位结构，最后一列存放分母多项式系数（变号、倒序）。
- $B$ 矩阵：按正序吸收分子多项式系数。

**注**：CCF 与 OCF 互为转置关系（见第 11 节对偶原理），工程实现时可复用同一套求解代码。

### 4. 能观性分析

能观性回答的问题是：**传感器能否从输出 $y$ 中重构系统的全部内部状态？**

#### 4.1 输出矩阵判据（对角化系统）

- **全零列**：若 $C$ 中某状态对应列全为 0，该状态无任何传感器通道，不可观。
- **线性相关列**：若 $C$ 的某两列成比例（如 $c_1 = [1,3]^T$ 与 $c_2 = [2,6]^T$），对应两状态在输出端线性混合、无法解耦，系统损失观测自由度，不可观。
- **单输出系统**：$C$ 为行向量时，只要元素非零，各列间不存在"复制"关系，不触发上述退化。

#### 4.2 传递函数零极点相消（结构性盲区）

设传递函数 $G(s) = N(s)/D(s)$。若 $N(s)$ 与 $D(s)$ 存在公因式（零极点相消），则相消的模态在传递函数中不可见：

- 相消后系统阶数降低（$D(s)$ 最高次幂减小），被消模态在物理世界中**仍然存在且可能发散**。
- 但输出中不含该模态分量，基于约分后模型设计的算法对其"失明"，系统可能在数据看似正常时失控。

**工程对策（容错控制雏形）**：保留全阶未约分模型，对被消/不可观模态设计状态观测器（影子系统），利用可观部分通过算法重构隐身状态，实现基于模型的状态估计兜底。

---

## 第二部分 稳定性分析：李雅普诺夫方法

### 5. 基本思想

传统判稳依赖特征值求解；李雅普诺夫第二方法从能量视角判定：若系统运动过程中存在一个正定的能量函数 $V(x)$，其沿系统轨迹的变化率恒为负（能量持续耗散至零），则系统渐近稳定。该方法无需显式求解系统响应，适用于线性与非线性、连续与离散系统。

### 6. 定号性

设 $V(x)$ 为标量函数，定义于包含原点的邻域：

- **正定**：$V(0)=0$，且 $x \neq 0$ 时 $V(x) > 0$。
- **半正定**：$V(x) \ge 0$，且存在非零点使 $V(x)=0$。
- **负定 / 半负定**：$-V(x)$ 正定 / 半正定。

**二次型与对称矩阵**：判定高阶系统稳定性需将能量函数写成矩阵二次型 $V(x) = x^T P x$。构造对称矩阵 $P$ 的规则：

- 平方项系数填入主对角线；
- 交叉项系数**除以 2** 后对称填入非对角线位置。

仅实对称矩阵的特征值必为实数，其正负号与二次型的定号性一一对应（特征值全正 ⇔ 正定），物理上对应能量函数在各方向上的弯曲程度。

### 7. 连续时间系统的李雅普诺夫方程

对线性定常系统 $\dot{x} = Ax$，取 $V(x) = x^T P x$，其导数为 $\dot{V} = x^T(A^T P + PA)x$。令

$$
A^T P + P A = -Q
$$

其中 $Q$ 为指定的正定对称矩阵（工程上常取 $Q = I$）。

**判定定理**：

1. **正向判定**：给定正定 $Q$，解方程得 $P$。若 $P$ 正定，则系统渐近稳定。
2. **逆向判定**：给定正定 $P$，反解 $Q$。若 $Q$ 正定，则系统渐近稳定（常用于控制器设计的边界验证）。
3. **半正定 $Q$ 的补充条件（扩展定理）**：若解得 $Q$ 仅半正定，只要系统矩阵 $A$ 与 $Q$ 满足完全能观条件（沿系统轨迹不存在恒使 $x^T Q x = 0$ 的非零状态），系统仍渐近稳定——动态过程会把停留在"死角"的状态重新拉入耗散通道。

**物理角色映射**：$A$ 为已知的系统矩阵；$P$ 为待求的能量函数形状；$Q$ 为人为指定的能量耗散速率。

### 8. 离散时间系统的李雅普诺夫判定

计算机控制与 ROS 2 节点均为离散系统（采样周期 $T$），状态方程为 $x_{k+1} = G x_k$。离散系统考察相邻两拍的能量差：

$$
\Delta V_k = x_{k+1}^T P x_{k+1} - x_k^T P x_k
$$

展开得到离散李雅普诺夫方程：

$$
G^T P G - P = -Q
$$

**求解要点**：指定正定 $Q$（如 $I$），按"先算 $G^T P G$，再减 $P$"的矩阵乘法顺序展开，解线性方程组求 $P$；$P$ 正定则系统渐近稳定。

**等价判据**：离散李雅普诺夫方程有正定解的充要条件是 $G$ 的全部特征值位于复平面单位圆内（模长小于 1）——与连续系统"特征值位于左半平面"相对应。

---

## 第三部分 系统综合

### 9. 状态反馈与极点配置

从被动分析走向主动干预：通过设计控制律改变闭环系统动力学。

**状态反馈**：$u = -K x$。代入系统方程得闭环系统

$$
\dot{x} = (A - BK)x
$$

通过设计反馈增益矩阵 $K$，可将闭环矩阵 $A - BK$ 的极点（特征值）配置到复平面左半平面的任意指定位置，使发散系统变为稳定且动态特性符合预期的系统。

**充要条件**：状态反馈可实现极点任意配置 ⇔ 系统 $(A, B)$ **完全能控**。若系统不完全能控，其不能控模态不受反馈影响，软件层面无法补偿，须回到硬件/结构层面解决。

**工程实现**：应试场景常用待定系数法展开特征多项式求解 $K$；工程实践中直接调用数值工具：

```python
import scipy.signal as signal
K = signal.place_poles(A, B, desired_poles).gain_matrix
```

### 10. 闭环状态观测器（龙伯格观测器）

物理系统通常无法为每个状态安装传感器，需在控制器所在的计算单元内构建状态估计器。

**开环观测器的缺陷**：$\dot{\hat{x}} = A\hat{x} + Bu$ 完全依赖模型"盲推"。受初始状态误差与模型参数漂移影响，估计误差将随时间累积（其演化受原系统稳定性支配），估计值最终与真实状态脱节——不能直接使用。

**闭环观测器（工业标准）**：引入输出误差反馈修正估计：

$$
\dot{\hat{x}} = A\hat{x} + Bu + H(y - C\hat{x})
$$

其中 $H$ 为观测器增益矩阵，$y - C\hat{x}$ 为实测输出与估计输出的残差。定义估计误差 $e = x - \hat{x}$，其动力学为

$$
\dot{e} = (A - HC)e
$$

**增益设计**：若系统 $(A, C)$ 完全能观，可通过极点配置将 $A - HC$ 的极点置于负半平面足够深处，使估计误差快速收敛。

**调参权衡（工程要点）**：观测器极点负实部越大，估计收敛越快，但对传感器噪声的放大越剧烈，可能导致执行器输出抖动甚至损坏。工程经验：**观测器极点实部通常取控制器极点的 2–5 倍**，兼顾收敛速度与噪声抑制。

### 11. 分离定理与对偶原理

**分离定理**：当控制器基于状态估计值 $\hat{x}$（而非真实 $x$）实施反馈 $u = -K\hat{x}$ 时，闭环系统特征值集合等于"控制器极点集合（$A-BK$）"与"观测器极点集合（$A-HC$）"的并集——两者代数上完全解耦。因此控制器与观测器可**独立设计、独立调参**，互不干扰。

**对偶原理**：能控性分析与能观性分析互为转置关系（$(A,B)$ 能控 ⇔ $(A^T, B^T)$ 能观）。工程红利：观测器增益无需重新实现算法——将 $A$、$C$ 转置后送入极点配置接口，所得结果再转置即为观测器增益 $H$。

### 12. 线性二次型调节器（LQR）

当系统状态维数较高时，人工指定全部极点位置不再可行。LQR 将设计问题转化为最优化问题：

$$
J = \int_0^\infty \left( x^T Q x + u^T R u \right) dt
$$

其中 $Q$ 惩罚状态偏离（响应质量），$R$ 惩罚控制能量（执行器代价）。最优反馈 $u = -K x$ 的增益由代数黎卡提方程给出：

$$
A^T P + P A - P B R^{-1} B^T P + Q = 0, \quad K = R^{-1} B^T P
$$

**工程要点**：LQR 不要求设计者指定极点，只需根据控制目标权衡 $Q$ 与 $R$（如：对状态偏差敏感则增大 $Q$，对能耗/执行器寿命敏感则增大 $R$）。求解由数值库完成（如 `scipy.linalg.solve_continuous_are`）。LQR 是自动驾驶、工业控制等现代控制系统的标准入场工具。

---

## 附录：工程工具链

| 用途 | 工具 |
|---|---|
| 极点配置 | `scipy.signal.place_poles(A, B, poles)` |
| 连续/离散 Riccati 方程 | `scipy.linalg.solve_continuous_are` / `solve_discrete_are` |
| 李雅普诺夫方程求解 | `scipy.linalg.solve_lyapunov` / `solve_discrete_lyapunov` |
| 仿真验证 | Python + NumPy/SciPy；ROS 2 + Gazebo（系统级） |

---

*文档维护：本文档随学习与工程实践持续修订。各模块对应的数值验证脚本与实验记录见仓库其余文件。*
